startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: 'uvx',
      args: ['--from', 'git+https://github.com/vdalhambra/siteaudit-mcp.git', 'siteaudit'],
    })
